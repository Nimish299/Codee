#include <Bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    int arr[n][n];
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            cin >> arr[i][j];
        }
    }
    vector<vector<int>> ans(n, vector<int>(n));
      for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            if(ans)
        }
    }
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {

            if (arr[i][j] == 0)
            {
                for (int row = 0; row < n; row++)
                {
                    for (int cl = 0; cl <= n; cl++)
                    {
                    }
                }
            }
        }
    }
}